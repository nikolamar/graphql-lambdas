overwrite: true
schema: "src/graphql/schemas/*"
documents: null
generates:
  src/graphql/generated.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  graphql.schema.json:
    plugins:
      - "introspection"
