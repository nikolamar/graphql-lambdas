mutation userPasswordAuth ($clientId: String!, $username: String!, $password: String!) {
  userPasswordAuth (clientId: $clientId, username: $username, password: $password) {
    session
    challengeName
    idToken
    accessToken
    refreshToken
  }
}