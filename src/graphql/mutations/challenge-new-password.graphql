mutation challengeNewPassword ($clientId: String!, $session: String!, $username: String!, $newPassword: String!) {
  challengeNewPassword (clientId: $clientId, session: $session, username: $username, newPassword: $newPassword) {
    idToken
    accessToken
    refreshToken
  }
}