overwrite: true
schema: "schemas/*"
documents: null
generates:
  generated.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  graphql.schema.json:
    plugins:
      - "introspection"
