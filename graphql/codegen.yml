overwrite: true
schema: "src/schemas/*"
documents: null
generates:
  src/generated.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  src/graphql.schema.json:
    plugins:
      - "introspection"
